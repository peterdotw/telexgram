import styled from "styled-components";

const StyledParagraph = styled.p``;

const AboutText = () => (
  <StyledParagraph>
    Telexgram is the best chat app in the universe. (kind of)
  </StyledParagraph>
);

export default AboutText;
