import styled from "styled-components";

const StyledParagraph = styled.p`
  font-size: 14px;
  font-weight: 300;
  margin-top: 40px;
`;

const Text = () => {
  return <StyledParagraph>best chat app in the universe.</StyledParagraph>;
};

export default Text;
